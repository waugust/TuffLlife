require 'spec_helper'

describe Armor do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Armor.create!(@valid_attributes)
  end
end
